# Environment variables example file
# Copy this file to .env and fill in your actual values

# OpenAI相关配置（如果需要使用AI功能）
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# 邮件转发机器人配置
# 源邮箱配置（用于接收邮件）
SOURCE_EMAIL=your_source_email@163.com
SOURCE_PASSWORD=your_imap_auth_code_here

# 目标邮箱配置（用于转发邮件）
TARGET_EMAIL=your_target_email@example.com

# SMTP配置（用于发送邮件，如果与源邮箱相同可不设置）
# SMTP_PASSWORD=your_smtp_auth_code_here

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/email_forwarder.log

# IMAP服务器配置（如果不使用163邮箱，需要修改）
# SOURCE_IMAP_SERVER=imap.163.com
# SOURCE_IMAP_PORT=993

# SMTP服务器配置（如果不使用163邮箱，需要修改）
# SMTP_SERVER=smtp.163.com
# SMTP_PORT=465